<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >

    <item
        android:id="@+id/menu_edit_room_phrase"
        android:icon="@drawable/ic_toolbar_phrase_list_24"
        app:showAsAction="ifRoom"
        android:visible="false"
        android:title="delete_room">
    </item>

    <item
        android:id="@+id/menu_edit_room"
        android:icon="@drawable/ic_toolbar_edit_24"
        app:showAsAction="ifRoom"
        android:visible="false"
        android:title="delete_room">
    </item>

    <item
        android:id="@+id/menu_delete_room"
        android:icon="@drawable/ic_toolbar_exit_24"
        app:showAsAction="ifRoom"
        android:visible="false"
        android:title="delete_room">
    </item>

</menu>